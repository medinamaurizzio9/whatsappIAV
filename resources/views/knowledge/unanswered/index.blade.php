@extends('layouts.app')
@section('title', 'Preguntas pendientes')
@section('content')
<div class="mb-3"><a href="{{ route('unanswered-questions.index') }}" class="btn btn-sm btn-outline-dark">Todas</a> <a href="{{ route('unanswered-questions.index', ['status' => 'pendiente']) }}" class="btn btn-sm btn-outline-warning">Pendientes</a> <a href="{{ route('unanswered-questions.index', ['status' => 'corregida']) }}" class="btn btn-sm btn-outline-success">Corregidas</a> <a href="{{ route('unanswered-questions.index', ['status' => 'descartada']) }}" class="btn btn-sm btn-outline-secondary">Descartadas</a></div>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0 align-middle"><thead><tr><th>Fecha</th><th>Pregunta</th><th>Intencion</th><th>Motivo</th><th>Estado</th><th class="text-end">Acciones</th></tr></thead><tbody>@forelse($questions as $question)<tr><td>{{ $question->created_at->format('d/m/Y H:i') }}</td><td>{{ Str::limit($question->question, 70) }}</td><td>{{ $question->intention?->name ?? '-' }}</td><td>{{ $question->reason }}</td><td><span class="badge text-bg-secondary">{{ $question->status }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('unanswered-questions.show', $question) }}">Ver</a> <a class="btn btn-sm btn-outline-dark" href="{{ route('unanswered-questions.edit', $question) }}">Editar</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Sin preguntas pendientes.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $questions->links() }}</div>
@endsection
