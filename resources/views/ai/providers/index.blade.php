@extends('layouts.app')
@section('title', 'Proveedores IA')
@section('content')
<div class="d-flex justify-content-end mb-3"><a href="{{ route('ai-providers.create') }}" class="btn btn-dark">Nuevo proveedor</a></div>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0 align-middle"><thead><tr><th>Nombre</th><th>Proveedor</th><th>Modelo</th><th>API Key</th><th>Estado</th><th class="text-end">Acciones</th></tr></thead><tbody>@foreach($providers as $provider)<tr><td>{{ $provider->name }} @if($provider->is_default)<span class="badge text-bg-warning">Default</span>@endif</td><td>{{ $provider->provider }}</td><td>{{ $provider->model }}</td><td>{{ $provider->maskedApiKey() }}</td><td><span class="badge text-bg-{{ $provider->is_active ? 'success' : 'secondary' }}">{{ $provider->is_active ? 'Activo' : 'Inactivo' }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('ai-providers.show', $provider) }}">Ver</a> <a class="btn btn-sm btn-outline-dark" href="{{ route('ai-providers.edit', $provider) }}">Editar</a> <form method="POST" action="{{ route('ai-providers.destroy', $provider) }}" class="d-inline" onsubmit="return confirm('Eliminar proveedor?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Eliminar</button></form></td></tr>@endforeach</tbody></table></div></div><div class="mt-3">{{ $providers->links() }}</div>
@endsection
