@extends('layouts.app')
@section('title', 'Prompts IA')
@section('content')<div class="d-flex justify-content-end mb-3"><a href="{{ route('ai-prompts.create') }}" class="btn btn-dark">Nuevo prompt</a></div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Nombre</th><th>Tipo</th><th>Estado</th><th class="text-end">Acciones</th></tr></thead><tbody>@foreach($prompts as $prompt)<tr><td>{{ $prompt->name }}</td><td>{{ $prompt->type }}</td><td><span class="badge text-bg-{{ $prompt->is_active ? 'success' : 'secondary' }}">{{ $prompt->is_active ? 'Activo' : 'Inactivo' }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('ai-prompts.show', $prompt) }}">Ver</a> <a class="btn btn-sm btn-outline-dark" href="{{ route('ai-prompts.edit', $prompt) }}">Editar</a> <form method="POST" action="{{ route('ai-prompts.destroy', $prompt) }}" class="d-inline" onsubmit="return confirm('Eliminar prompt?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Eliminar</button></form></td></tr>@endforeach</tbody></table></div></div><div class="mt-3">{{ $prompts->links() }}</div>@endsection
